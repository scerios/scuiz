<div class="row mt-5">
  <div class="col-md-6 m-auto">
    <div class="card card-body">
      <h1 class="text-center mb-3">
        <i class="fas fa-user-plus"></i> <%- welcomeMsg %>
      </h1>
      <form action="/players/register" method="POST">
        <div class="form-group">
          <label for="name"><%- nameLabel %></label>
          <input
            type="name"
            id="name"
            name="name"
            class="form-control"
            placeholder="<%- namePlaceHolder %>"
            value="<%= typeof name != 'undefined' ? name : '' %>"
          />
        </div>
        <div class="form-group">
          <label for="password"><%- password %></label>
          <input
            type="password"
            id="password"
            name="password"
            class="form-control"
            placeholder="<%- passwordPlaceholder %>"
            value="<%= typeof password != 'undefined' ? password : '' %>"
          />
        </div>
        <div class="form-group">
          <label for="password2"><%- confirmPassword %></label>
          <input
            type="password"
            id="confirmPassword"
            name="confirmPassword"
            class="form-control"
            placeholder="<%- confirmPasswordPlaceholder %>"
            value="<%= typeof confirmPassword != 'undefined' ? password2 : '' %>"
          />
        </div>
        <button type="submit" class="btn btn-primary btn-block">
          <%- registerBtn %>
        </button>
      </form>
      <p class="lead mt-4"><%- isRegisteredQuestion %> <a href="/players/login"><%- loginLink %></a></p>
    </div>
  </div>
</div>